package com.github.sguzman.binding.scala.typesafe.verify

case class Payments(
                   allowChildMatching: Boolean,
                   name: String,
                   nav: EarningsNav,
                   primarySideNav: Int
                   )
